const availableOrders = ['id', 'is_on_trip', 'location_name'];
const sortTypes = ['asc', 'desc'];

const { cars } = require('./constants');

const orderById = (input, sortType) => {

};

const orderByTripStatus = (input, sortType) => {

};

const orderByLocationName = (input, sortType) => {

};

switch(orderType){
	case 'id':
	break;

	case 'is_on_trip':
	break;

	case 'location_name':
	break;

	default: 
}

module.exports = sortedData;